import React from "react";
import { View, Text, Image } from "react-native";

const RestaurantCard = ({ image, restaurantName, address, stars, time }) => {
  return (
    <View>
      <Image />
    </View>
  );
};

export default RestaurantCard;
